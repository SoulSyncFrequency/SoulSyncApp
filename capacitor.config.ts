import { CapacitorConfig } from '@capacitor/cli'
const config: CapacitorConfig = { appId:'com.soulsync.app', appName:'SoulSync', webDir:'dist', server:{ androidScheme:'https' } }
export default config
