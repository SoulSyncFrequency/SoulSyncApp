import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.therapyengine.app',
  appName: 'TherapyEngine',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
