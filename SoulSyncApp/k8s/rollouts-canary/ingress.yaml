apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: soulsync-app
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - app.<your-domain>
      secretName: soulsync-tls
  rules:
    - host: app.<your-domain>
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: soulsync-app-stable
                port:
                  number: 80
