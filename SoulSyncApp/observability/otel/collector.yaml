receivers:
  otlp:
    protocols:
      http:
        endpoint: 0.0.0.0:4318

exporters:
  otlphttp/tempo:
    endpoint: http://tempo:3200

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlphttp/tempo]
