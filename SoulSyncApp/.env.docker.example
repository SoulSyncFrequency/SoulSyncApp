# ==== SoulSync Docker Environment (example) ====
# Copy as `.env` when running `docker compose up`

# App
NODE_ENV=production
PORT=3000

# Database (matches docker-compose.yml)
DATABASE_URL=postgresql://soulsync:soulsync@db:5432/soulsync

# CORS / Frontend
FRONTEND_ORIGIN=http://localhost:5173

# Admin
ADMIN_IP_ALLOWLIST=127.0.0.1,::1

# SMTP (optional)
SMTP_HOST=smtp.example.com
SMTP_USER=mailer@example.com
SMTP_PASS=changeme
SMTP_FROM=SoulSync <no-reply@example.com>

# Health check
HEALTHZ_URL=http://localhost:3000/healthz

# Sentry (optional)
VITE_SENTRY_DSN=

# JWT / Secrets (replace in production)
JWT_SECRET=change_me_dev
REFRESH_TOKEN_SECRET=change_me_dev_refresh
