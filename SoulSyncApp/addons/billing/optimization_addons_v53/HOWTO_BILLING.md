Set ENV (STRIPE_SECRET_KEY, STRIPE_PRICE_ID, STRIPE_WEBHOOK_SECRET, APP_BASE_URL, ENTITLEMENTS_REPO=pg).
Protect premium routes with requireEntitlement.
Use Hosted Checkout (no extra scripts; CSP remains strict).
